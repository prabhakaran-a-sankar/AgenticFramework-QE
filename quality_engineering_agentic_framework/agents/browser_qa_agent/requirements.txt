# Core agent
playwright>=1.44.0
pydantic-settings>=2.0.0
aiofiles>=23.0.0
jinja2>=3.1.0
httpx>=0.28.1

# LLM providers (install only what you need)
anthropic>=0.30.0           # LLM_PROVIDER=anthropic
openai>=1.0.0               # LLM_PROVIDER=openai or azure_openai
google-generativeai>=0.7.0  # LLM_PROVIDER=gemini
# Ollama uses plain httpx — no extra package needed

# Job queue
celery>=5.3.0
redis>=5.0.0

# Database
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0           # SQLite async (dev)
asyncpg>=0.29.0             # PostgreSQL async (prod)
alembic>=1.13.0

# Artifact storage
boto3>=1.34.0               # S3 / R2 / MinIO / GCS

# CLI
typer>=0.12.0
rich>=13.0.0

# GitHub App integration
PyJWT>=2.8.0
cryptography>=42.0.0

# Mobile native (optional — only for Appium/native iOS/Android)
# Appium-Python-Client>=3.1.0
